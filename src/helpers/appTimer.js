// const startTimer = (loopFunc, reState) => reState({ activeLoop: loopFunc });

// const pauseTimer = (loopFunc, reState) => {
//   clearInterval(loopFunc);
//   reState({ activeLoop: null });
// };

// module.exports = { startTimer, pauseTimer };
